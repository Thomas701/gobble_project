CC = gcc
EXEC = tp
SRC = $(wildcard *.c)
OBJ = $(SRC:.c=.o)

all : $(EXEC)

%.o : %.c
	$(CC) -o $@ -c $<

$(EXEC) : $(OBJ)
	$(CC) -o $@ $^ -lm
	rm -rf *.o

main.o : fonction.h
main.o : prototype.h
fonction.o : prototype.h 
clear :
	rm -rf *.o

mrproper : clear
	rm -rf $(EXEC)